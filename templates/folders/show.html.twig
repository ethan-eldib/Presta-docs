{% extends 'base.html.twig' %}

{% block title %}Folders{% endblock %}

{% block body %}
    <h1>Folders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ folder.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ folder.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('folders_index') }}">back to list</a>

    <a href="{{ path('folders_edit', {'id': folder.id}) }}">edit</a>

    {{ include('folders/_delete_form.html.twig') }}
{% endblock %}
