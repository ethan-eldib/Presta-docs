<nav class="navbar navbar-expand-lg">
	<a class="navbar-brand" href="{{ path('homepage') }}">Presta-doc</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path('homepage') }}">Accueil
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href=" {{ path('folders_new') }}">Déposer un document
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('pack_doc') }}">Packs presta-doc
				</a>
			</li>
	
			<li class="nav-item">
				<a class="nav-link" href="{{ path('contact') }}">Contact
				</a>
			</li>
		</ul>
		<ul class="navbar-nav ml-auto">
			{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('account_logout') }}">Déconnexion</a>
				</li>
				<span class="navbar-divider-line">|</span>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('my_account') }}">Mon compte</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('register') }}">Inscription</a> 
				</li>
				<span class="navbar-divider-line">|</span>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('account_login') }}">Connexion</a>
				</li>
			{% endif %}
				<span class="navbar-divider-line">|</span>
		</ul>
		<a href="{{ path('cart') }}">
			<img class="icon-default-style" src="{{ asset('images/icons/shopping-cart.png') }}" alt="Shopping cart icon from Flaticon">
		</a>
	</div>
</nav>

{# Message flash #}
{% include "partials/_flash_message.html.twig" %}
